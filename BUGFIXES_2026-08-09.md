# 버그 수정 기록 (2026-08-09)

승객/기사/회사 대시보드를 코드 추적으로 감사해서 찾은 버그 중, 재현 경로를 직접 검증한 6건을 수정했다. (Explore 에이전트 3개로 총 13개 후보를 찾았고, 그중 확정된 것만 반영 — 나머지 7건은 "보류 항목" 참고)

## 수정한 버그 6건

### 1. 회사 계정 "내 정보"에서 후기 탭·차량사진 갤러리 미노출
- **파일:** `components/bidder/BidderProfileTabPanel.tsx`
- **증상:** 회사(BusCompany) 계정으로 로그인해 "나의 정보"에 들어가면 "상세내역/후기" 탭 전환 자체가 안 뜨고, 차량 사진 갤러리도 안 보임.
- **원인:** `showReviews` 조건이 `role === 'driver'`로 하드코딩되어 있었음. 회사 대시보드도 기사와 동일하게 `onProfileSectionChange`/`driverReviews`/`vehiclePhotos`를 다 채워서 넘기는데 역할 체크 때문에 렌더링이 막혀 있었음.
- **수정:** `role` 조건 제거, `onProfileSectionChange != null`만으로 판단.

### 2. 회사 "입찰중" 탭에서 왕복 여정 매칭이 깨짐
- **파일:** `components/company/CompanyDashboardContent.tsx`
- **증상:** 회사가 왕복 여정 중 한쪽 다리에만 입찰한 상태에서 "계약 > 입찰중" 탭을 보면 왕복 배지·합산 거리가 표시되지 않음(기사 계정은 정상 표시).
- **원인:** `ContractOpenBidsList`에 넘기는 `partnerPool`이 `c.myBids`(이미 입찰한 것만)로 좁게 설정되어 있고 `roundOptions`도 누락되어, 아직 입찰 전인 반대쪽 다리를 찾지 못했음.
- **수정:** `partnerPool={c.openTripsPool}`, `roundOptions={c.COMPANY_ROUND_OPTS}`로 기사쪽과 동일하게 맞춤.

### 3. 승객 견적 카드 "⋮" 메뉴가 다이얼로그를 닫아도 계속 열려있음
- **파일:** `components/passenger/PassengerQuoteTripsList.tsx`
- **증상:** 견적 카드의 "⋮" 메뉴에서 "여정 수정"/"주문 취소"를 눌러 다이얼로그를 열었다가 그냥 닫아도, 메뉴가 펼쳐진 채로 남아있음.
- **원인:** 메뉴를 닫는 콜백(부모가 이미 `setCancelMenuTripId(null)` 호출)에 이어서 같은 클릭 핸들러 안에서 `onToggleCancelMenu(trip.id)`(함수형 토글)를 또 호출 — React가 같은 배치에서 두 업데이트를 순서대로 적용하며 결과가 뒤집혀 메뉴가 다시 열림 상태가 됨.
- **수정:** 이미 부모 콜백이 메뉴를 닫고 있으므로 중복 토글 호출 제거.

### 4. 입찰 상세를 빠르게 여러 번 열면 다른 기사의 후기가 표시될 수 있음
- **파일:** `hooks/usePassengerDashboard.tsx` (`openBidDetail`)
- **증상:** 입찰 상세를 연달아 빠르게 클릭하면, 네트워크 응답 순서가 뒤바뀌어 화면에 보이는 입찰과 다른 기사의 후기·평점이 표시될 수 있음.
- **원인:** 비동기 응답이 오면 무조건 state를 덮어써서, 먼저 보낸 요청이 나중에 도착하면 최신 상태를 덮어버림(경쟁 상태).
- **수정:** 요청별 증가 카운터(`useRef`)를 두고, 응답이 최신 요청일 때만 state를 반영하도록 가드 추가.

### 5. 회사 계정은 '주문' 탭 재진입/창 포커스 시 자동 새로고침이 안 됨
- **파일:** `hooks/useCompanyDashboard.tsx`
- **증상:** 다른 탭에 갔다가 '주문' 탭으로 돌아오거나 브라우저 탭을 다시 포커스해도 목록이 갱신되지 않음(기사 계정은 정상 동작).
- **원인:** 기사 훅에는 있는 `'available'` 탭 재진입 리로드 + 창 focus/visibilitychange 리로드 effect가 회사 훅에는 없었음.
- **수정:** 기사 훅과 동일한 두 `useEffect`를 회사 훅에 추가.

### 6. 날짜 필터가 자정~오전 9시 출발 여정을 하루 전으로 잘못 취급
- **파일:** `lib/tripFilters.ts`
- **증상:** KST 00:00~08:59 출발 여정을 그 날짜로 필터링해도 목록에서 빠짐.
- **원인:** 필터 비교에 `new Date(...).toISOString().slice(0,10)`(UTC 기준)를 사용 — `<input type="date">`에서 온 로컬 날짜와 어긋남.
- **수정:** UTC 변환 대신 `getFullYear`/`getMonth`/`getDate`로 로컬 날짜 키를 만드는 `toLocalDateKey` 헬퍼로 교체.

## 함께 반영한 UI 개선 (같은 세션)

- `components/layout/DashboardMobileShell.tsx`: 헤더의 이모지·유니코드 아이콘(☰/←/⌂)을 `lucide-react` 아이콘으로 교체, 터치 타겟 44px 이상 확보
- `components/passenger/PassengerQuoteRequestSection.tsx`: "준비중" placeholder 3칸을 실제 액션 카드보다 아래·작게 배치해 정보 위계 정리
- `components/support/SupportInquiryDialog.tsx` / `components/company/CompanyDashboardContent.tsx`: 회사 전용 문의 메뉴(`COMPANY_SUPPORT_MENU`) 추가 — 기존엔 기사용 문구를 그대로 재사용하고 있었음
- `hooks/usePassengerTripForm.ts` / `components/passenger/PassengerTripCreateFlow.tsx`: 견적 등록 폼을 제출 성공/취소 시 초기화하지 않던 문제 수정 — 이전 입력값이 다음 견적 등록 때 그대로 남아있던 버그

## 보류 항목 (이번엔 손대지 않음)

1. 왕복 여정 생성 중 두 번째 다리 생성 실패 시 롤백 없음 (`usePassengerTripForm.ts` `createTrip`)
2. 왕복 여정 취소 중 일부만 실패해도 목록 리로드 안 함 (`usePassengerDashboard.tsx` cancel 로직)
3. 여정 수정 다이얼로그에 과거 날짜 검증 없음 (등록 플로우엔 있음)
4. 여정 수정 인원수 입력에 음수 방어 미흡 (백엔드가 막아서 데이터 오염은 없음)
5. 카카오 장소검색 디바운스/취소 없음 — 빠른 타이핑 시 응답 역전 가능
6. 입찰 철회(withdraw) 버튼에 진행중 가드 없음 — 느린 네트워크에서 중복 클릭 가능
7. 회사 필터 초기화 버튼이 목록 리로드를 안 함(기사는 함)

## 검증

- `npx tsc --noEmit`, `npm run build`(프론트/백엔드) 통과
- 변경 파일 대상 `eslint` — 기존에 있던 경고 외 새 에러 없음
- 브라우저로 직접 확인: 회사 계정 후기 탭 노출, 승객 "⋮" 메뉴 재오픈 안 됨, 견적 등록 폼 재오픈 시 빈 값, 날짜 필터 정상 매칭(회귀 없음)
