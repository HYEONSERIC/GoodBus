# TurnstileError 400020 — `/signup-business` 가입 차단 이슈 및 Turnstile 제거 (2026-09-05)

이 문서는 2026-09-05 하루 동안 발견·조사·결정·구현·검증까지 이어진 전체 경위를 기록한다. 처음엔 "조치 없이 보류"로 결정했으나, 이후 대화 중 Turnstile의 실효성 자체를 재검토하면서 최종적으로 **코드 레벨에서 완전히 제거**하는 것으로 결론이 바뀌었다.

## 1. 발견 경위

Sentry(`javascript-nextjs` 프로젝트)에서 `TurnstileError: [Cloudflare Turnstile] Error: 400020` 이슈를 확인 — `/signup-business`(기사·회사 가입) 페이지, production 환경, 28 events(첫 발생 약 하루 전). 사용자가 실제 화면·Sentry 이벤트 상세·Cloudflare Turnstile 대시보드 스크린샷을 공유하며 함께 조사했다.

## 2. 증상

- 가입 화면에서 Turnstile 위젯이 정상적으로 검증되지 않음 — 어떤 시점엔 "Troubleshoot" 링크가 노출되고, 어떤 시점엔 위젯 자체가 아무 UI 없이 조용히 실패
- "인증번호 전송" 버튼이 비활성 상태로 유지되거나, 진행해도 마지막 제출 단계에서 막힘
- **실제 재현 로그**: 전화번호 `01090109684`로 SMS 인증번호(`7980`)가 정상 발송·수신되고 입력까지 완료했음에도, 최종 제출 시 "보안 인증을 완료해주세요"(Turnstile 미검증) 에러로 가입이 완료되지 않음

## 3. 원인 조사

에러 코드 `400020`은 Cloudflare 공식 문서(`developers.cloudflare.com/turnstile/troubleshooting/client-side-errors/error-codes/`) 기준 **"Invalid sitekey"**, non-retryable — 즉 한번 발생하면 재시도로 해결되지 않는 에러.

자체 설정을 3가지 확인했고 전부 정상이었다:

| 확인 항목 | 결과 |
|---|---|
| 프로덕션 빌드에 박힌 site key | `/_next/static/chunks/18r8pk4-8mtjg.js`에서 `0x4AAAAAAAEbCe26oE9BQ7CLY` 확인 |
| Cloudflare 대시보드 Site key (`busrent_bs` 위젯) | `0x4AAAAAAAEbCe26oE9BQ7CLY` — **완전 일치** |
| 위젯 등록 Hostname | `busrent.co.kr` — 실제 접속 도메인과 **정확히 일치** |
| Widget Mode | `Managed`(권장값, 정상) |
| 프론트 코드 (`components/auth/TurnstileWidget.tsx`) | 하드코딩 fallback 없음, `NEXT_PUBLIC_TURNSTILE_SITE_KEY` env 값을 그대로 사용 — 코드 결함 없음 |

설정에 문제가 없다는 걸 확인한 뒤 Cloudflare 커뮤니티를 검색한 결과, 서로 무관한 다수 계정에서 **"실제(prod) sitekey는 400020으로 실패하는데 Cloudflare 공식 테스트 sitekey는 같은 페이지에서 정상 작동한다"**는 동일한 증상을 보고하고 있었다. 위젯을 삭제 후 재생성해도 안 고쳐졌다는 보고도 다수. 이는 우리 쪽 설정 문제가 아니라 **Cloudflare Turnstile 서비스 자체의 산발적 버그**일 가능성이 높다는 뜻이다.

다만 Cloudflare 공식 상태 페이지(cloudflarestatus.com)에는 이 건이 정식 인시던트로 등록돼 있지 않다 — free 티어 이슈가 상태페이지에 누락되는 경우가 흔해서, "지금 이 순간 전 세계적으로 장애 중"이라고 100% 확정할 근거는 없고, 예상 복구 시점도 알 수 없다.

## 4. 영향 범위

- **영향 있음**: `/signup-business`(기사·회사 신규 가입) — Turnstile은 이 경로에만 적용됨(`server/src/routes/auth.ts`, `role !== 'Passenger'`일 때만 검증)
- **영향 없음**: 승객 가입/로그인, 기사·회사 로그인, 그 외 전체 기능
- 알리고(Aligo) SMS 발송 자체는 정상 동작 — 사용자가 인증번호를 받고 입력해도 마지막 Turnstile 단계에서 가입이 완료되지 않음 (SMS 비용은 발생하지만 가입 전환은 안 됨)

## 5. 1차 결정 (보류) — 이후 재검토로 뒤집힘

처음엔 "`/signup-business`가 실사용 중이 아니므로 별도 조치 없이 그대로 두고 나중에 재확인"으로 결정했다. 이때 검토했던 대안(Turnstile 키만 임시로 비우기)은 실사용 임박 전까지 불필요한 보안 완화라고 보고 보류했었다.

## 6. 재검토 — Turnstile 배치 자체의 타당성 의문

사용자가 "SMS는 실제 돈이 나가는데, 이메일 로그인과 달리 이걸 끄면 비용 어뷰징에 더 취약해지는 거 아니냐"고 문제를 제기하면서 코드를 다시 추적, 다음 사실이 드러났다:

- **Turnstile을 붙인 원래 이유는 이미 무효화된 상태였다.** 2026-08-15 도입 당시 근거는 "`signup-business`(기사·회사)만 전화OTP 비용장벽이 없다"는 것이었는데, 나흘 뒤인 2026-08-19에 전 역할이 전화번호+OTP로 통일되면서 이 전제 자체가 사라졌다. 이후에도 아무도 Turnstile을 재검토하지 않고 방치된 상태였다.
- **실제 비용이 나가는 지점을 Turnstile이 보호하지 않고 있었다.** `server/src/routes/auth.ts`를 다시 보면, 진짜 SMS 발송(`sendOtpSms`)은 `POST /phone/request-otp`에서 일어나는데 이 라우트엔 Turnstile 검증이 전혀 없다(IP 레이트리밋만 있음). Turnstile은 그보다 훨씬 뒤, 이미 OTP를 다 받고 코드까지 입력한 뒤의 `POST /signup` 최종 제출에서만 체크되고 있었다 — 즉 "봇이 SMS를 스팸으로 발송시키는 것"은 애초에 못 막고, "SMS까지 이미 받은 사람이 계정을 최종 생성하는 것"만 막는 구조였다.
- 결론적으로 Turnstile은 (a) 원래 목적은 이미 사라졌고, (b) 위치도 실제 비용 발생 지점과 어긋나 있어 방어 효과가 낮은데, (c) 지금처럼 Cloudflare 쪽 버그로 가입 경로 전체를 막아버릴 수 있는 장애 리스크만 남아있는 상태였다.

## 7. 최종 결정 (2026-09-05) — 코드까지 완전 제거

**얻는 방어 효과 대비 장애 리스크가 더 크다고 판단해, Turnstile을 임시 비활성화가 아니라 코드 레벨에서 완전히 제거하기로 결정.**

제거해도 아래 방어선은 그대로 남아있어 "완전 무방비"는 아님:
- IP당 OTP 요청 10분 10회 제한 (`otpRequestRateLimiter`, `server/src/routes/auth.ts`)
- IP당 가입 1시간 10회 제한 (`signupRateLimiter`)
- 번호당 OTP 쿨다운 60초 + 10분 내 5회 + 일일 20회 캡 (`server/src/utils/otp.ts`)
- 실제 SMS 코드 입력 필수(코드 5회 시도 제한, 5분 만료)
- Cloudflare 전체 프록시(Phase 3)의 CDN/원본 IP 은닉/기본 방화벽은 Turnstile과 무관하게 그대로 유지

### 구현 내역

| 구분 | 파일 | 내용 |
|---|---|---|
| 삭제 | `server/src/utils/turnstile.ts` | `verifyTurnstileToken` 유틸 전체 삭제 |
| 삭제 | `components/auth/TurnstileWidget.tsx` | 위젯 컴포넌트 전체 삭제 |
| 수정 | `server/src/routes/auth.ts` | import·`turnstileToken` 스키마 필드·검증 블록 제거 |
| 수정 | `app/signup-business/page.tsx` | 위젯 렌더·`turnstileToken` 상태·제출 전 검사·API 호출 파라미터 제거 |
| 수정 | `lib/api.ts` | `authAPI.signup` 파라미터 타입에서 `turnstileToken` 제거 |
| 수정 | `.env.production.example`, `server/.env.example` | 관련 안내 주석 삭제 |
| 수정 | 로컬 `.env.local`, `server/.env` | 실제 키 값도 함께 삭제(git 미추적 파일) |
| 문서 | `CLAUDE.md` | Turnstile 아키텍처 패턴 서술 삭제 |
| 문서 | `PROJECT_STATUS.md` | 2026-08-15/08-25 관련 항목에 취소선+제거 사실 추가, 새 완료 항목(2026-09-05) 추가, 최종 갱신일 갱신 |

### 검증 (`/verify` 스킬, 런타임 구동 기준)

- `npx tsc --noEmit`(루트+서버) 클린, `npm run lint` 0 에러(기존 이미지 관련 경고 20개만 무관하게 존재)
- 리포지토리 전체 `grep -rn turnstile` — 실제 코드/env에는 0건(남은 건 이 보고서와 `SECURITY_FIXES_2026-08-20.md`의 역사 기록, `PROJECT_STATUS.md`의 취소선 처리된 히스토리뿐)
- 로컬 서버(:3000 프론트/:4000 백엔드) 실제 기동 후 API를 직접 구동해 확인:
  - `/signup-business` 페이지 HTML+JS 청크에 Turnstile 흔적 0건(스크립트·마크업 완전 제거 확인)
  - **버스회사(BusCompany) 가입 풀 플로우**(원래 Turnstile이 유일하게 걸려있던 역할) — OTP 요청→코드 입력→가입까지 `200` 정상 완료
  - 기사(Driver)·승객(Passenger) 가입도 정상 완료(회귀 없음)
  - 🔍 옛날 클라이언트가 보낼 수 있는 `turnstileToken` 필드를 그대로 실어 보내도 Zod가 조용히 무시하고 정상 처리(스키마 에러 없음)
  - 🔍 같은 번호로 다른 이름을 넣어 재가입 시도 → 기존 dedupe 로직대로 새 계정 생성 없이 기존 계정 로그인 처리(DB에 중복 row 없음까지 직접 조회 확인)
  - 🔍 `BusCompany` 필수 필드 누락, 잘못된 OTP 코드 — 기존 검증 로직 그대로 정상 거부(회귀 없음)
  - 테스트로 생성한 계정은 전부 정리, 로컬 서버도 종료 완료

### Cloudflare 인프라 영향 없음 확인

이번 변경은 애플리케이션 코드만 건드렸고 `deploy/nginx/*`나 Cloudflare 대시보드 설정은 전혀 손대지 않았다. 실제 프로덕션 도메인(`https://busrent.co.kr`)에 curl로 직접 확인:

- `server: cloudflare`, `cf-ray: a363e54e8962b6a2-LAX` 헤더 확인 — 트래픽이 여전히 Cloudflare 엣지를 정상 경유
- `x-frame-options`/`x-content-type-options`/`strict-transport-security`(HSTS)/`referrer-policy` 등 보안 헤더 전부 정상 응답(Turnstile과 무관한 nginx/next.config 레벨 설정이라 영향 없음)
- SSL 인증서 정상(`CN=busrent.co.kr`, `SSL certificate verify ok`)
- 원본 서버 IP(`172.237.7.249`) 직접 우회 접속 여전히 차단(연결 거부) — Phase 3 때 설정한 방어 그대로 유지
- 페이지 `HTTP 200` 정상 응답

즉 Cloudflare의 CDN 중계·기본 보안 헤더·SSL·원본 IP 은닉 기능은 이번 Turnstile 제거와 완전히 무관하게 그대로 정상 동작 중임을 실측으로 확인했다.

## 8. 남은 작업

1. **프로덕션 배포 필요** — 이 변경은 아직 로컬 작업트리에만 반영됨(커밋 전). 평소 배포 절차(`git pull` → `npm ci`(필요시) → `npm run build:prod` → `pm2 restart`)를 따르면 됨
2. Cloudflare 대시보드의 `busrent_bs` 위젯 자체 삭제는 선택 사항(코드에서 더 이상 참조하지 않으므로 안 지워도 위험 없음, 정리 차원)

## 9. 참고 링크

- [Cloudflare Turnstile 에러 코드 공식 문서](https://developers.cloudflare.com/turnstile/troubleshooting/client-side-errors/error-codes/)
- [Turnstile Error 400020 on all new widgets - test key works, production keys fail](https://community.cloudflare.com/t/turnstile-error-400020-on-all-new-widgets-test-key-works-production-keys-fail/911900)
- [All real Turnstile sitekeys return error 400020 on my account — official test sitekey works](https://community.cloudflare.com/t/all-real-turnstile-sitekeys-return-error-400020-on-my-account-official-test-sitek/951413)
- [Real Turnstile sitekeys return 400020 while official test sitekey works](https://community.cloudflare.com/t/real-turnstile-sitekeys-return-400020-while-official-test-sitekey-works/953716)
- [All Turnstile widgets fail with error 400020 (invalid sitekey) — official test key works](https://community.cloudflare.com/t/all-turnstile-widgets-fail-with-error-400020-invalid-sitekey-official-test-key/950807)
